# Python
__pycache__/
*.pyc

# macOS
.DS_Store

# 渲染输出 / 中间产物（不入库）
pose/renders/
render_out/
/tmp/
*.png

# 游戏资产（体积大且版权不属于本工程，切勿提交）
HoneySelect/
*.fbx
*.tar.gz
# 例外: pose/samples 的 Mixamo 动画样本 FBX 入库(README 引用, 供其他工程直接用);
# 被忽略的仍是游戏角色部件 FBX(body/head/hair/top/bot/shoe)
!pose/samples/*.fbx
